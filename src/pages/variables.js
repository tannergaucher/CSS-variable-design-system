import { Layout } from "../components"
import React from "react"

export default function VariablesPage({ location }) {
  return (
    <Layout location={location}>
      <article className="padding">
        <h1 className="h1">Variables</h1>
      </article>
    </Layout>
  )
}
