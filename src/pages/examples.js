import { Layout } from "../components"
import React from "react"

export default function Examples({ location }) {
  return (
    <Layout location={location}>
      <article className="padding container">
        <h1 className="h1">Examples</h1>
      </article>
    </Layout>
  )
}
